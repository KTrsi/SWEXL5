<% if flash[:notice] %>
   <%= flash[:notice] %>
<% end %>

<% @users.each do |e| %>
     <p>
      <%= e.uid %>
      <%= e.pass %>
      <%= link_to '削除', "/users/#{e.id}", method: :delete %>
    </p>
<% end %>
<%= link_to 'ユーザー登録', "/users/new" %><br>
<%= link_to 'メイン','/' %>